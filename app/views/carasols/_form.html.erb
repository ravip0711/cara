<%= form_for(@carasol) do |f| %>
  <% if @carasol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@carasol.errors.count, "error") %> prohibited this carasol from being saved:</h2>

      <ul>
      <% @carasol.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
